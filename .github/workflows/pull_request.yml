name: Check Pull Request

on: [pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Clone project
        uses: actions/checkout@v5

      - name: luatest1
        run: lua -v

      - name: Run luacheck
        uses: lunarmodules/luacheck@v1
        with:
          args: -q .

      - name: luatest2
        run: lua -v
